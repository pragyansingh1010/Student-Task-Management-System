# Task Filtering Rules

Filters should be applied to the current task collection without mutating stored tasks. Combining status and priority filters should narrow results predictably, while clearing filters restores the full list.